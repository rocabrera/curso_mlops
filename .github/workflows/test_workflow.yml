name: Primeiro Workflow

on:
  pull_request:
    - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Hello World
        run: echo Criando meu primeiro workflow

